description: wip tbd
type: object
properties:
  id:
    type: integer
    example: 0
    description: wip unique identifier for case
  rxId:
    type: string
    description: wip unique identifier for ***
    example: gG3pBCR1GCMKm89c
  caseData:
    oneOf:
      - $ref: ./IdData.yaml
      - $ref: ./WeightData.yaml
      - $ref: ./PreferredDoseData.yaml
