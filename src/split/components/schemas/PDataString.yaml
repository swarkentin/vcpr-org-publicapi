type: object
required:
  - promiseId
  - dat
properties:
  promiseId:
    description: set null or zero if User wishes to be anonymous
    type: integer
    default: null
    example: 123
  dat:
    type: string
    description: comment from a user
    example: this is
